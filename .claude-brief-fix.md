# Fix notebooks + scripts + soporte multi-lenguaje — Brief para Claude Code

## Contexto
El sitio está online en https://learning.technoova.es. El repo Learning tiene
5 sesiones: arquitectura-software/, agentes-ia/, algoritmia/,
ingenieria-de-requisitos/, ingenieria-de-software/. Algunas tienen archivos
que NO son .md (notebooks .ipynb, scripts .py, etc.) que necesitan verse en
la web.

Ya tenemos `site/scripts/notebooks.py` que convierte notebooks de algoritmia
a HTML estático. Hay que extender esto a TODA la repo + soportar cualquier
tipo de archivo con syntax highlighting automático.

## Bugs originales + nuevas features

### Bugs (siguen pendientes)
1. **Los notebooks NO se pueden abrir** — el Dockerfile tiene `rm -rf dist/algoritmia/notebooks`
   que borra los archivos generados antes de copiarlos al container nginx.
2. **Los archivos `.py` se descargan** — no se renderizan en pantalla.
3. **Catch-all o nginx fallback muestra página del sitio en vez del notebook**.

### Nueva feature: renderer universal
- **Cualquier extensión de archivo** en cualquier sesión debe poder renderizarse
  con syntax highlighting.
- Pygments soporta 500+ lenguajes — usar auto-detección cuando sea posible.
- Extensiones NO soportadas (imágenes, binarios) deben copiarse tal cual para descarga.

## Cambios esperados

### 1. Renombrar y reescribir `site/scripts/notebooks.py` → `site/scripts/assets.py`

**Estructura nueva del script:**

```python
#!/usr/bin/env python3
"""
Procesa todos los assets de las sesiones del repo Learning:
- Notebooks .ipynb → HTML estático self-contained (con outputs ejecutados)
- Archivos de código → HTML con syntax highlighting (Pygments)
- Archivos no-código (.txt, .json, .csv, etc.) → HTML con highlighting o texto plano
- Binarios (imágenes, etc.) → copia tal cual

Output por sesión en public/<session>/<asset-type>/<slug>/index.html
Output combinado: public/<session>/_index.json con metadata de todos los assets
"""
```

**Funciones clave:**

```python
ASSET_EXTENSIONS = {
    # código con highlighting especial
    '.py': 'python', '.js': 'javascript', '.ts': 'typescript',
    '.jsx': 'jsx', '.tsx': 'tsx',
    '.rs': 'rust', '.go': 'go', '.java': 'java',
    '.c': 'c', '.cpp': 'cpp', '.h': 'c', '.hpp': 'cpp',
    '.rb': 'ruby', '.php': 'php', '.sh': 'bash', '.bash': 'bash',
    '.sql': 'sql', '.html': 'html', '.css': 'css', '.scss': 'scss',
    '.json': 'json', '.yaml': 'yaml', '.yml': 'yaml', '.toml': 'toml',
    '.xml': 'xml', '.md': 'markdown', '.txt': 'text',
    '.csv': 'csv', '.tsv': 'csv',
    # binarios que se copian tal cual (descarga)
    # (se detectan por extensión en find_assets)
}

BINARY_EXTENSIONS = {'.png', '.jpg', '.jpeg', '.gif', '.svg', '.webp', '.pdf', '.zip', '.tar', '.gz'}

SESSIONS = [
    'arquitectura-software',
    'agentes-ia',
    'algoritmia',
    'ingenieria-de-requisitos',
    'ingenieria-de-software',
]

def find_assets(session_dir: Path) -> dict:
    """Busca notebooks y archivos de código en una sesión."""
    assets = {'notebooks': [], 'code': [], 'binary': []}
    for f in sorted(session_dir.rglob('*')):
        if not f.is_file():
            continue
        if f.suffix == '.ipynb':
            assets['notebooks'].append(f)
        elif f.suffix.lower() in BINARY_EXTENSIONS:
            assets['binary'].append(f)
        elif f.suffix.lower() in ASSET_EXTENSIONS:
            assets['code'].append(f)
        # Ignorar .md (ya se manejan en sync-content.mjs)
    return assets

def slugify(name: str) -> str:
    return name.rsplit('.', 1)[0].replace('_', '-').replace(' ', '-').lower()

def render_code_to_html(filepath: Path, language: str) -> Path:
    """Renderiza archivo de código a HTML con Pygments."""
    from pygments import highlight
    from pygments.lexers import get_lexer_by_name, guess_lexer
    from pygments.formatters import HtmlFormatter
    from pygments.util import ClassNotFound

    code = filepath.read_text(encoding='utf-8', errors='replace')

    # Intentar lexer por nombre, si no por guess
    try:
        lexer = get_lexer_by_name(language)
    except ClassNotFound:
        try:
            lexer = guess_lexer(code)
        except ClassNotFound:
            lexer = get_lexer_by_name('text')

    formatter = HtmlFormatter(
        style='monokai',
        full=True,
        linenos='table',
        lineanchors='line',
        anchorlinenos=True,
        cssclass='highlight',
    )
    body = highlight(code, lexer, formatter)

    # Plantilla HTML simple
    page = f"""<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{filepath.name} - Learning</title>
  <style>
    body {{ font-family: system-ui, -apple-system, sans-serif; max-width: 1000px; margin: 0 auto; padding: 2rem; background: #fafafa; color: #18181b; }}
    pre, code {{ font-family: 'Fira Mono', 'Cascadia Code', 'Courier New', monospace; font-size: 0.875rem; line-height: 1.5; }}
    .linenos {{ color: #888; user-select: none; }}
    a.back {{ display: inline-block; margin-bottom: 1.5rem; color: #2563eb; text-decoration: none; font-size: 0.875rem; }}
    a.back:hover {{ text-decoration: underline; }}
    h1 {{ font-size: 1.5rem; margin: 0 0 0.5rem; color: #18181b; }}
    .meta {{ color: #71717a; font-size: 0.875rem; margin-bottom: 1.5rem; }}
    .meta code {{ background: #f4f4f5; padding: 0.125rem 0.375rem; border-radius: 0.25rem; font-size: 0.8125rem; }}
  </style>
</head>
<body>
  <a href="../" class="back">← Volver</a>
  <h1>{filepath.name}</h1>
  <p class="meta">
    Lenguaje: <code>{lexer.name}</code>
    · Tamaño: <code>{filepath.stat().st_size / 1024:.1f} KB</code>
    · Solo lectura
  </p>
  {body}
</body>
</html>"""
    return body, page

def convert_notebook(nb_path: Path) -> Path:
    """Convierte .ipynb a HTML self-contained con nbconvert."""
    # (misma lógica que ya está en notebooks.py — no romper)
    pass

def main():
    for session in SESSIONS:
        session_dir = REPO_ROOT / session
        if not session_dir.exists():
            continue

        assets = find_assets(session_dir)
        session_meta = []

        # Procesar notebooks
        for nb in assets['notebooks']:
            slug = slugify(nb.name)
            out_dir = PUBLIC_DIR / session / 'notebooks' / slug
            out_file = convert_notebook(nb, out_dir)
            session_meta.append({
                'type': 'notebook',
                'slug': slug,
                'title': nb.stem,
                'filename': nb.name,
                'url': f'/{session}/notebooks/{slug}/',
                'cells': get_cell_count(nb),
            })

        # Procesar archivos de código
        for f in assets['code']:
            slug = slugify(f.name)
            language = ASSET_EXTENSIONS.get(f.suffix.lower(), 'text')
            out_dir = PUBLIC_DIR / session / 'files' / slug
            out_file = out_dir / 'index.html'
            _, page = render_code_to_html(f, language)
            out_file.write_text(page, encoding='utf-8')
            session_meta.append({
                'type': 'file',
                'slug': slug,
                'title': f.stem,
                'filename': f.name,
                'language': language,
                'url': f'/{session}/files/{slug}/',
                'size_kb': round(f.stat().st_size / 1024, 1),
            })

        # Procesar binarios (copia tal cual)
        for f in assets['binary']:
            slug = slugify(f.name)
            out_dir = PUBLIC_DIR / session / 'binaries'
            out_dir.mkdir(parents=True, exist_ok=True)
            shutil.copy2(f, out_dir / f.name)
            session_meta.append({
                'type': 'binary',
                'slug': slug,
                'title': f.stem,
                'filename': f.name,
                'url': f'/{session}/binaries/{f.name}',
                'size_kb': round(f.stat().st_size / 1024, 1),
            })

        # Guardar índice de la sesión
        if session_meta:
            meta_file = PUBLIC_DIR / session / '_index.json'
            meta_file.write_text(
                json.dumps(session_meta, indent=2, ensure_ascii=False),
                encoding='utf-8',
            )
            print(f"✅ {session}: {len(session_meta)} assets → {meta_file.relative_to(SITE_DIR)}")
```

### 2. `site/Dockerfile`

QUITAR la línea `rm -rf dist/algoritmia/notebooks` del RUN final. Agregar `pygments`
al pip install:

```dockerfile
RUN pip3 install --no-cache-dir --break-system-packages \
    nbconvert jupyter-client ipykernel pygments
```

Y el RUN queda:
```dockerfile
ENV LEARNING_REPO_ROOT=/repo-sessions
RUN node scripts/sync-content.mjs && \
    python3 scripts/assets.py && \
    npm run build
```

### 3. `site/src/pages/[session].astro`

Modificar para:
- Leer `public/<session>/_index.json` (siempre, no solo para algoritmia)
- Mostrar sección "📓 Notebooks" (si hay)
- Mostrar nueva sección "📁 Archivos" (si hay) con grid de cards: nombre + lenguaje + tamaño
- Mostrar nueva sección "🖼️ Recursos" (si hay binarios, opcional)

Adaptar el HTML existente. La estructura del card de archivo:

```astro
<a
  href={f.url}
  class="group block rounded-lg border border-zinc-200 bg-white p-4 transition hover:border-blue-500 hover:shadow-md dark:border-zinc-800 dark:bg-zinc-900 dark:hover:border-blue-500"
>
  <div class="mb-2 flex items-center gap-2">
    <span class="inline-flex h-6 w-6 items-center justify-center rounded bg-zinc-100 text-xs font-bold text-zinc-700 dark:bg-zinc-800 dark:text-zinc-300">
      {/* emoji según extensión */}
    </span>
    <h3 class="text-base font-semibold text-zinc-900 group-hover:text-blue-600 dark:text-zinc-100 dark:group-hover:text-blue-400">
      {f.filename}
    </h3>
  </div>
  <p class="text-xs text-zinc-500 dark:text-zinc-400">
    {f.language} · {f.size_kb} KB
  </p>
</a>
```

### 4. `site/package.json`

Actualizar el script build:
```json
"build": "node scripts/sync-content.mjs && python3 scripts/assets.py && astro build && pagefind --site dist"
```

Y agregar `"assets": "python3 scripts/assets.py"` como script independiente.

### 5. Limpieza

- Borrar `site/scripts/notebooks.py` (reemplazado por `assets.py`)
- Borrar `site/public/algoritmia/notebooks/scripts/` (los .py Commiteados ya no se sirven)
- NO borrar `site/public/algoritmia/notebooks/_index.json` (se regenerará con assets.py)
- NO borrar `site/public/algoritmia/notebooks/time/index.html` ni `ordenamiento/index.html` (se regenerarán)

## Verificación final

```bash
cd site
rm -rf src/content dist public/algoritmia public/agentes-ia public/arquitectura-software public/ingenieria-de-requisitos public/ingenieria-de-software
python3 scripts/assets.py
ls -la public/algoritmia/
ls -la public/algoritmia/notebooks/time/
ls -la public/algoritmia/files/  # si hay archivos
cat public/algoritmia/_index.json | python3 -m json.tool
npm run build
echo "---dist check---"
ls -la dist/algoritmia/notebooks/time/
ls -la dist/algoritmia/files/  # si hay
grep -oE '<title>[^<]+</title>' dist/algoritmia/notebooks/time/index.html
grep -oE '<title>[^<]+</title>' dist/algoritmia/files/quickshort/index.html  # si existe
```

## Cosas que NO debes hacer
- NO hacer git add/commit/push
- NO tocar archivos fuera de site/
- NO modificar nginx.conf (no hace falta)
- NO eliminar el catch-all `[...slug].astro` (los archivos estáticos tienen prioridad porque nginx los sirve primero si existen)
- NO cambiar la estructura del repo root

## Idioma
- Comentarios en español
- Strings de UI en español

Empezá. Si algo no queda claro, parame y preguntame.